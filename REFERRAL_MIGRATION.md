# Миграция реферальных ссылок

## Изменения в структуре хранения

### Было:
В таблице `referral_links` хранилась полная ссылка:
```
http://192.168.1.4:3000/register?ref=cmig6qhzf00023l6n9k7pqeyt
```

### Стало:
В таблице `referral_links` хранится только `user_id`:
```
cmig6qhzf00023l6n9k7pqeyt
```

## Преимущества новой структуры

1. **Гибкость**: При изменении домена студенческого сайта не нужно обновлять все ссылки в БД
2. **Централизованное управление**: URL берется из настройки `STUDENT_WEB_SERVER_URL`
3. **Простота**: Храним минимум данных, остальное формируется динамически

## Как работает

### Backend (API)
```typescript
// Получаем user_id из БД
const referralLink = await prisma.referral_links.findFirst({
  where: { teacher_id: user.id }
})

// Формируем полную ссылку
const studentWebServerUrl = await getSetting('STUDENT_WEB_SERVER_URL', 'https://bilimpoz.kg')
const fullReferralLink = `${studentWebServerUrl}/register?ref=${referralLink.referral_link}`

// Отправляем на фронтенд
return { referralLink: fullReferralLink }
```

### Frontend
Компоненты получают уже готовую полную ссылку и используют её для:
- Отображения на странице
- Копирования в буфер обмена
- Отправки в Telegram
- Отправки в WhatsApp

## Миграция существующих данных

### Шаг 1: Убедитесь, что настройка STUDENT_WEB_SERVER_URL добавлена в БД

```sql
INSERT INTO settings (key, value, created_at, updated_at)
VALUES ('STUDENT_WEB_SERVER_URL', 'https://bilimpoz.kg', NOW(), NOW())
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
```

### Шаг 2: Запустите миграцию

```bash
npm run migrate:referral-links
```

Скрипт автоматически:
- Найдет все реферальные ссылки в БД
- Извлечет `user_id` из полных URL
- Обновит записи, сохранив только `user_id`
- Пропустит уже мигрированные записи

### Пример вывода:

```
🔄 Начинаем миграцию реферальных ссылок...
📊 Найдено 5 реферальных ссылок
✅ Обновлено: http://192.168.1.4:3000/register?ref=user123 -> user123
✅ Обновлено: https://bilimpoz.kg/register?ref=user456 -> user456
✓ Пропущено (уже в правильном формате): user789

📈 Результаты миграции:
   ✅ Обновлено: 2
   ⏭️  Пропущено: 3
   📊 Всего: 5

✨ Миграция завершена успешно!
```

## Проверка работы

### 1. Проверьте API
```bash
curl http://localhost:3000/api/teacher/referral \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Ответ должен содержать полную ссылку:
```json
{
  "referralLink": "https://bilimpoz.kg/register?ref=cmig6qhzf00023l6n9k7pqeyt",
  "stats": { ... }
}
```

### 2. Проверьте страницу студентов
- Откройте `/students`
- Реферальная ссылка должна отображаться полностью
- Кнопка "Копировать" должна работать
- Кнопки Telegram и WhatsApp должны отправлять правильную ссылку

### 3. Проверьте отправку в мессенджеры

**Telegram:**
```
Присоединяйся к Bilimpoz! Используй мою реферальную ссылку:

https://bilimpoz.kg/register?ref=cmig6qhzf00023l6n9k7pqeyt
```

**WhatsApp:**
```
Присоединяйся к Bilimpoz! Используй мою реферальную ссылку: https://bilimpoz.kg/register?ref=cmig6qhzf00023l6n9k7pqeyt
```

## Откат (если нужно)

Если нужно вернуться к старой структуре:

```sql
-- Обновите все ссылки, добавив префикс
UPDATE referral_links 
SET referral_link = 'https://bilimpoz.kg/register?ref=' || referral_link
WHERE referral_link NOT LIKE 'http%';
```

## Важные замечания

1. **Не меняйте `user_id` в таблице `referral_links`** - это сломает существующие ссылки
2. **Обновите `STUDENT_WEB_SERVER_URL`** в настройках при смене домена
3. **Миграция безопасна** - можно запускать несколько раз, уже мигрированные записи будут пропущены



