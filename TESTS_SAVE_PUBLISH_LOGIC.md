# Логика кнопок "Сохранить" и "Опубликовать" в разделе тестов

Детальное описание всех действий, которые происходят после нажатия на кнопки сохранения и публикации теста.

---

## 🔖 Кнопка "Сохранить"

### Расположение
- Верхняя панель редактора теста
- Иконка закладки (BookmarkIcon)
- Текст: "Сохранить" / "Saving..." (при сохранении)

### Условия активации
- Тест существует (`test` не null)
- Учитель авторизован (`teacherId` существует)
- Кнопка блокируется во время сохранения (`saving = true`)

---

### Процесс сохранения (handleSaveTest)

#### 1. **Инициализация**
- Устанавливается флаг `saving = true` (блокировка UI)
- Проверяется наличие теста и teacherId

#### 2. **Определение ID теста**
- Если тест новый (нет ID или `isNewTest`):
  - Генерируется временный ID через `generateTempId()` (формат: `temp-{timestamp}-{random}`)
- Если тест существует:
  - Используется существующий ID

#### 3. **Сохранение теста в localStorage**
Создается объект черновика:
```typescript
{
  id: testId,
  name: test.name || 'Новый тест',
  description: test.description || '',
  language: test.language,
  status: 'draft',
  createdAt: test.createdAt || текущая дата,
  updatedAt: текущая дата,
  teacherId: teacherId
}
```

Действия:
- `saveDraftTest(draftTest)` - сохраняет в `teacher_tests_draft`
- `setTestStatus(testId, 'draft')` - устанавливает статус в `test_statuses`

#### 4. **Обновление состояния теста**
- Если тест новый:
  - Обновляется состояние теста с новым ID
  - Обновляется `questionsCount` из количества вопросов
  - Если ID изменился, переносятся вопросы из старого ID в новый через `TestLocalStorage.saveTestQuestions()`
  - Обновляется URL через `router.replace()` на новый ID
- Если тест существующий:
  - Обновляется только `updatedAt`

#### 5. **Синхронизация вопросов (задержка 100ms)**
Дается задержка для сохранения данных из TestBlock компонентов в localStorage.

#### 6. **Обработка каждого вопроса**

Для каждого вопроса выполняется:

**a) Определение типа вопроса**
- Маппинг старых типов на новые:
  - `single` → `standard`
  - `reading` → `rac`
  - `comparison` → `standard`

**b) Загрузка данных из localStorage**
- Приоритет у данных из `TestLocalStorage.load()` (TestBlock сохраняет автоматически)
- Если данных нет в localStorage, используются данные из state

**c) Проверка на пустоту вопроса**
Вопрос считается полностью пустым если:
- Нет текста вопроса
- Нет вариантов ответов ИЛИ все варианты пустые
- Нет изображения

Если вопрос полностью пустой:
- Удаляется из localStorage через `TestLocalStorage.remove()`
- Пропускается в цикле сохранения

**d) Проверка на частичное заполнение**
Вопрос считается частично заполненным если есть:
- Текст вопроса ИЛИ
- Хотя бы один заполненный вариант ответа ИЛИ
- Изображение

**e) Сохранение вопроса**
Если вопрос частично заполнен:
- Формируется объект данных:
  ```typescript
  {
    question: текст вопроса,
    answers: массив вариантов с id, value, isCorrect,
    points: очки (по умолчанию 1),
    timeLimit: время (по умолчанию 60),
    imageUrl: URL изображения,
    explanation_ai: AI объяснение,
    language: язык вопроса
  }
  ```
- Сохранение через `TestLocalStorage.save(questionId, data, type)`
- Обновление state вопроса с правильным типом
- Увеличение счетчика `savedQuestionsCount`

#### 7. **Удаление пустых вопросов из state**
- Фильтрация вопросов: остаются только те, у которых есть данные в localStorage
- Обновление `questions` state
- Логирование количества удаленных вопросов

#### 8. **Сохранение списка вопросов теста**
- Формируется массив ID вопросов с типами
- Проверяется наличие данных в localStorage для каждого вопроса
- Сохранение через `TestLocalStorage.saveTestQuestions(testId, questionIds)`
- Это нужно для правильного подсчета вопросов на карточках тестов

#### 9. **Сброс флага несохраненных изменений**
- Проверяется, что тест сохранен в localStorage
- Если сохранен: `setHasUnsavedChanges(false)`

#### 10. **Уведомление пользователя**
- Если сохранены вопросы: "Тест и {count} вопросов сохранены"
- Если только тест: "Тест сохранен"
- При ошибке: показывается сообщение об ошибке

#### 11. **Завершение**
- `setSaving(false)` - разблокировка UI

---

## 🚀 Кнопка "Опубликовать"

### Расположение
- Верхняя панель редактора теста
- Иконка самолета (PaperAirplaneIcon)
- Текст: "Опубликовать" / "Publishing..." (при публикации)
- Зеленая кнопка

### Условия отображения
- Тест существует
- Статус теста = `draft`
- Есть хотя бы один вопрос
- Кнопка блокируется во время публикации

---

### Процесс публикации

#### Этап 1: handlePublishTest (первичная проверка)

**1.1. Базовая валидация**
- Проверка наличия теста, вопросов и teacherId
- Если нет вопросов → ошибка "Добавьте вопросы перед публикацией"

**1.2. Валидация всех вопросов (validateAllQuestions)**

Выполняется полная валидация каждого вопроса:

**Проверка 0: Количество вопросов**
- Должно быть ровно **30 вопросов**
- Если 0 → ошибка "Добавьте вопросы"
- Если не 30 → ошибка "Должно быть 30 вопросов, сейчас {count}"

**Проверка 1: Заполненность вопроса**
Для каждого вопроса проверяется:
- Есть текст вопроса ИЛИ
- Есть изображение ИЛИ
- Есть текст для чтения (textRac) - для типа RAC
- Если ничего нет → ошибка "Вопрос {номер}: Заполните текст или добавьте фото"

**Проверка 2: Количество вариантов ответов**
- Минимум 2 варианта ответа
- Если меньше → ошибка "Вопрос {номер}: Минимум 2 варианта ответа"

**Проверка 3: Заполненность всех вариантов**
- Все варианты ответов должны быть заполнены (не пустые)
- Если есть пустые → ошибка "Вопрос {номер}: Все варианты должны быть заполнены"
- После фильтрации пустых должно остаться минимум 2 варианта

**Проверка 4: Правильный ответ**
- Должен быть выбран хотя бы один правильный ответ
- Если нет → ошибка "Вопрос {номер}: Выберите правильный ответ"

**Результат валидации:**
- Если есть ошибки → показываются все ошибки списком
- Если валидация пройдена → переход к модальному окну подтверждения

**1.3. Модальное окно подтверждения**
- Заголовок: "Подтвердите публикацию"
- Сообщение: предупреждение о публикации
- Кнопки: "Опубликовать" (подтверждение) и "Отмена"
- Тип: `warning` (желтый)

**1.4. Запуск публикации**
После подтверждения:
- Закрытие модального окна
- Задержка 100ms
- Вызов `executePublishTest()`

---

#### Этап 2: executePublishTest (основной процесс)

**2.1. Инициализация**
- `setSaving(true)` - блокировка UI
- Повторная проверка наличия теста, вопросов и teacherId

**2.2. Определение типа теста**
- Проверка: является ли тест черновиком (`isTempId` или `isDraftOnly`)
- Сохранение старого ID теста

**2.3. Сохранение/обновление теста в БД**

**Если тест черновик (временный ID):**
- **POST** `/api/teacher/tests`
- Отправляются данные:
  ```json
  {
    "name": название теста,
    "description": описание,
    "teacherId": ID учителя,
    "language": язык теста
  }
  ```
- Получается новый реальный ID из ответа
- `savedTestId` = новый ID из БД

**Если тест уже в БД:**
- **PUT** `/api/teacher/tests/{testId}`
- Обновляются название, описание, язык
- `savedTestId` = существующий ID

**2.4. Сохранение вопросов в БД**

Сохраняются старые ID вопросов для последующего удаления из localStorage.

Для каждого вопроса:

**a) Синхронизация данных из localStorage**
- Загрузка актуальных данных из `TestLocalStorage.load()`
- Приоритет у данных из localStorage
- Формирование `questionData` с актуальными данными

**b) Валидация перед сохранением в БД**
- Проверка наличия текста вопроса
- Проверка минимум 2 заполненных вариантов
- Проверка наличия правильного ответа
- При ошибке → увеличение `failedCount`, сохранение последней ошибки, переход к следующему вопросу

**c) Сохранение вопроса в БД**

**Если вопрос с временным ID (isTempId):**
- **POST** `/api/teacher/tests/{savedTestId}/questions?teacherId={teacherId}`
- Отправляются данные:
  ```json
  {
    "question": текст вопроса,
    "textRac": текст для чтения,
    "answerVariants": массив вариантов (фильтруются пустые),
    "photoUrl": URL изображения,
    "points": очки,
    "timeLimit": время,
    "explanationAi": AI объяснение,
    "language": язык,
    "type": тип вопроса
  }
  ```
- При успехе:
  - Увеличение `savedCount`
  - Обновление ID вопроса в state (замена временного на реальный)
- При ошибке:
  - Увеличение `failedCount`
  - Сохранение сообщения об ошибке

**Если вопрос уже в БД:**
- **PUT** `/api/teacher/tests/{savedTestId}/questions/{questionId}?teacherId={teacherId}`
- Отправляются те же данные
- При успехе: увеличение `savedCount`
- При ошибке: увеличение `failedCount`

**2.5. Проверка результата сохранения вопросов**
- Если `failedCount > 0`:
  - Если все вопросы не сохранены → показывается последняя ошибка
  - Если частично → показывается "Сохранено {saved} из {total} вопросов" + последняя ошибка
  - Прерывание процесса публикации
- Если все вопросы сохранены → продолжение

**2.6. Обновление статуса теста в БД**
- **PATCH** `/api/teacher/tests/{savedTestId}/status?teacherId={teacherId}`
- Отправляется: `{ "status": "published" }`
- При ошибке → исключение с сообщением

**2.7. Обновление состояния теста**
- Обновляется `test` state:
  - `id` = `savedTestId` (новый или существующий)
  - `status` = `'published'`
  - `questionsCount` = количество вопросов

**2.8. Очистка localStorage**

После успешной публикации удаляются все временные данные:

**a) Удаление черновика теста**
- Если тест был черновиком → `removeDraftTest(oldTestId)`

**b) Удаление списка вопросов теста**
- Удаляется `testQuestions_{oldTestId}` из localStorage
- Если ID изменился → удаляется `testQuestions_{savedTestId}`

**c) Удаление данных всех вопросов**
- Для каждого старого ID вопроса вызывается `TestLocalStorage.remove(id, type)`
- Удаляются все данные вопросов из localStorage

**d) Удаление статуса теста**
- `removeTestStatus(oldTestId)` - удаление статуса для старого ID
- Если ID изменился → `removeTestStatus(savedTestId)` - удаление для нового ID
- Статус теперь хранится в БД, не в localStorage

**2.9. Обновление URL**
- Если `savedTestId !== test.id` → `router.replace(/dashboard/tests/${savedTestId})`
- URL обновляется на реальный ID из БД

**2.10. Уведомление пользователя**
- При успехе: "Тест опубликован"
- При ошибке: показывается сообщение об ошибке

**2.11. Завершение**
- `setSaving(false)` - разблокировка UI

---

## 📊 Сравнительная таблица

| Параметр | Сохранить | Опубликовать |
|----------|-----------|--------------|
| **Хранение теста** | localStorage (черновик) | БД (опубликованный) |
| **Хранение вопросов** | localStorage | БД |
| **Валидация** | Минимальная (проверка на пустоту) | Полная (30 вопросов, все поля заполнены) |
| **Статус теста** | `draft` | `published` |
| **ID теста** | Временный (temp-xxx) | Реальный из БД |
| **ID вопросов** | Временные | Реальные из БД |
| **Очистка localStorage** | Нет | Да (после публикации) |
| **Модальное окно** | Нет | Да (подтверждение) |
| **Обновление URL** | Да (если новый тест) | Да (если ID изменился) |
| **Требование вопросов** | Нет | Да (ровно 30) |

---

## 🔄 Поток данных

### При сохранении:
```
UI изменения → TestBlock → localStorage → handleSaveTest → localStorage (обновление)
```

### При публикации:
```
localStorage → validateAllQuestions → executePublishTest → БД → очистка localStorage
```

---

## ⚠️ Важные особенности

1. **Автосохранение вопросов**: TestBlock компоненты автоматически сохраняют данные в localStorage при каждом изменении
2. **Приоритет localStorage**: При сохранении приоритет у данных из localStorage, а не из state
3. **Временные ID**: Новые тесты и вопросы получают временные ID (temp-xxx), которые заменяются на реальные при публикации
4. **Строгая валидация**: При публикации требуется ровно 30 полностью заполненных вопросов
5. **Очистка localStorage**: После публикации все временные данные удаляются из localStorage
6. **Гибридное хранение**: Черновики в localStorage, опубликованные в БД
7. **Маппинг типов**: Старые типы вопросов автоматически конвертируются в новые

---

**Дата создания:** 2025-01-16  
**Версия:** 1.0

